# Local runtime only. Never commit populated credentials.
APP_OWNER_EMAIL=
BILLING_MODE=test
APP_ORIGIN=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
PROVIDER_ALLOWED_HOSTS=

# Replicate is used only when BILLING_MODE=live and the generation tariff is approved.
REPLICATE_API_TOKEN=
# Random server secret, at least 32 characters, for authenticated per-job callbacks.
REPLICATE_WEBHOOK_SECRET=
